import { View } from 'react-native';

export const MapView = View as any;
export const Marker = View as any;
export const Polyline = View as any;
export type LatLng = { latitude: number; longitude: number };
export default MapView;
